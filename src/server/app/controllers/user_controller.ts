import { User } from "main";

export async function controllerCreateUser({ name, password }: User) {
  // const userExists = await ValidationUser({ name });
  // if (userExists) return { msg: "O usuário já existe" } as {msg: string };

  return 
}